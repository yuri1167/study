#基本の型はこう。
for i in range(10):
#elseは書かないといけないので、何も入れたくない時は代入先の変数に合わせて''とか0とか入れる
num = 0
	num += i if i==4 else 0
print(num)

#word1,2両方に当てはまる英単語だけ出力
word1 = 'Go is God'
word2 = 'ready Go Go'
listWord = word1.split(' ')
pWord = ''
for l in listWord:
#条件に当てはまらない場合''(空白)を入れればいい
	pWord += l if l in word2 and l not in pWord else ''	
print(Go)

#下記の場合も同じ
#リストの中の最大値を出力(max()関数を使わずに)
numList = [1,5,3,2,4]
maxnum = numList[0]
for num in numList:
    #a=aみたいな感じでそのまま値を渡せば三項演算子は成立する
    maxnum = num if maxnum<num else maxnum
print(5)
